import React from "react";

// * Style
import classes from "./style.module.css";

const Hero = () => (
  <div className={classes.Hero}>
    <h1 className={classes.bigHeader}>Information about Covid-19 around the world</h1>
  </div>
);

export default Hero;
